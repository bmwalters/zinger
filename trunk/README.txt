If you're using the SVN version, you'll need to run the "install pcf.bat" file. It installs the particles into the proper folder.

Once its been installed (or updated) you'll have to restart Garry's Mod so the particles will be loaded.
